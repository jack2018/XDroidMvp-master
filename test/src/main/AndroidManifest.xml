<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.droidlover.xdroidmvp.test">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">
        <activity
            android:name=".mvp.UiDelegateActivity"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.multi_p.BActivity"></activity>
        <activity android:name=".ImageLoaderTest"></activity>
         <activity android:name=".mvp.no_p.NoPActivity"></activity>
    </application>

</manifest>
